import 'package:flutter/material.dart';

class AuthorCollabScreen extends StatelessWidget {
  const AuthorCollabScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Author Collaboration Screen'),
    );
  }
}